# タスクリスト：Supabaseの従業員データの活用

- [x] `@supabase/supabase-js` パッケージのインストール
- [x] 環境変数（`.env`）の設定用ひな形（`.env.example`）の作成
- [x] `src/lib/supabase.js` の作成（Supabaseクライアントの初期化処理）
- [x] `App.jsx` 内で従業員データを取得するカスタムフックまたはuseEffectの実装
- [x] 日報実績入力部分の「名前」入力フィールドを、取得した従業員データを用いた選択式（`<select>` または `<datalist>`）に変更する
- [x] 動作確認
