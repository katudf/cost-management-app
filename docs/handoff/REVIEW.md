# REVIEW — 設計整合レビュー（claude）

対象: <REQUEST.md の対象>
日時: <YYYY-MM-DD HH:MM>

`CLAUDE.md`「設計上の重要なルール」との突き合わせ。
grep で拾えない部分を読む。

| 観点 | 判定 | 指摘 |
|---|---|---|
| Supabase呼び出しがフック経由か | | |
| 削除確認が `useConfirm()` か / `ConfirmModal` の配置 | | |
| マジック文字列（`constants.js` 使用） | | |
| 数値入力の `min="0"` | | |
| アイコンボタンの `aria-label` / `title` | | |
| エラー通知が `showToast` でユーザーに届くか | | |

判定は 適合 / 違反 / 対象なし のいずれか。

## その他の気づき

<既存パターンからの逸脱、命名の不整合、責務のずれなど。
 規約に書かれていなくても、読んで引っかかった点を書く。>

## 総合

- <問題なし / 要修正>
