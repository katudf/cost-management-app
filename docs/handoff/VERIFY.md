# VERIFY — 機械検証結果（agy）

対象: <REQUEST.md の対象>
日時: <YYYY-MM-DD HH:MM>

## 1. npm test

- 結果: <passed / failed>
- ファイル数 / テスト数: <n> / <n>  （ベースライン 2 / 26）
- 所要: <秒>
- 失敗した場合の出力:

```
```

## 2. npm run build

- 結果: <成功 / 失敗>
- 所要: <秒>  （ベースライン 約17秒）
- 新規の警告・エラー: <なし / 内容>
  ※ chunk 500kB 超の警告は既知なので報告不要

## 3. 規約 grep

| 項目 | ベースライン | 今回 | 判定 |
|---|---|---|---|
| `supabase.from` を直呼びするコンポーネント | 5件 | <n>件 | <増減なし / 増加> |
| `window.confirm` / `window.prompt` の実使用 | 0件 | <n>件 | |
| ステータス直書き | 0件 | <n>件 | |

増加した場合、**増えた分のファイルと行番号のみ**を列挙する。

## 総合

- <合格 / 要修正>
- 要修正の場合、何を直すべきか（直さない。指摘だけ）
