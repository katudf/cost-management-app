# タスクリスト：エクセルインポートの重複名制御

- [x] App.jsx に `aliasName` ステートを追加する
- [x] `handleExcelImport` の修正（重複チェックと `type` の設定）
- [x] インポート時のモーダルUIの修正（`type === 'duplicate'` 時の表示）
- [x] `handleImportChoice` の修正（`overwrite_duplicate` と `create_alias` の処理追加）
- [x] 動作確認（手動検証）
