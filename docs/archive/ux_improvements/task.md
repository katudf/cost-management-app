# タスクリスト: 画面の切り替えと文脈の分断解消 / 操作性向上

- [x] フェーズ1: 現場名ホバーによるサマリーツールチップ
- [x] フェーズ2: ポップアップからの「詳細画面ジャンプ機能」
- [x] 追加タスク: 現場名のドラッグ＆ドロップ配置
- [x] 追加タスク: 複数セルのコピー＆ペースト・削除 (キーボード対応)
- [x] 追加タスク: 1対多のコピペ展開

## 追加タスク: 会社行事/休日のセル操作機能の実装
- [x] `AssignmentChartTab.jsx` のデータ状態の変更: `companyHolidays` のデータ形式を単なる日付文字列の配列から `[{ date: '...', description: '...' }, ...]` のオブジェクト配列に変更。
- [x] 取得処理(`fetchAssignments`)の修正: `CompanyHolidays` の `select('date, description')` 取得に変更。
- [x] 会社行事セル用のポップアップ（メニュー）用ステートの追加。
- [x] 会社行事行（`<tr>`）のセルクリック時のハンドラ追加: クリックでポップアップを表示。
- [x] ポップアップUIの追加: 「休日」「会議」「社員旅行」「削除」のボタンを配置。
- [x] 登録・削除処理のAPIロジック実装: `supabase.from('CompanyHolidays')` に対する upsert および delete 処理。
- [x] セル描画ロジックの修正: `description` の内容に応じた色やテキスト（「休」「会議」「旅行」等）の出し分け。

## 追加タスク: 配置表の現場名の並び替え
- [x] `AssignmentChartTab.jsx` のデータ取得処理に `display_order` のソート条件を追加。
- [x] 現場名（`<td>`）に `onDragOver` と `onDrop` のドラッグ＆ドロップ受け入れ処理を追加。
- [x] ドロップ時に `barProjects` 配列を再配置し、`display_order` を再計算してデータベースに一括保存する処理（`handleProjectReorder`）の実装。
